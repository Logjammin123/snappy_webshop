- content_for :title, @category

%p
  %b Name:
  = @category.name

%p
  =link_to "Listing products of the category #{@category}", category_products_path(@category)

- if user_signed_in?
  = link_to 'Edit', edit_category_path(@category)
  |
  \#{link_to 'Back', categories_path}
